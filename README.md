# 🔥 jirgram

**Кастомный Telegram-клиент на базе TDLib с расширенными возможностями**

## 🚀 Возможности

- **👻 Ghost Mode**: скрытие онлайн-статуса, прочтений, набора текста
- **🛡️ Anti-Delete**: сохранение удаленных сообщений
- **📝 Message History**: история редактирования сообщений
- **🎨 Customization**: темы, настройки интерфейса
- **🔒 Privacy First**: без встроенных API ключей, все через конфиг
- **⚡ Fast**: построен на TDLib - официальной библиотеке Telegram

## 📦 Установка

### Требования

- Python 3.8+
- TDLib (устанавливается автоматически через pip)

### Быстрый старт

```bash
# Клонируем репозиторий
git clone https://github.com/yanealomora-eng/jirgram.git
cd jirgram

# Устанавливаем зависимости
pip install -r requirements.txt

# Копируем конфиг
cp config.example.py config.py

# Редактируем config.py и добавляем свои API credentials
# Получить можно на https://my.telegram.org

# Запускаем
python main.py
```

## 🔑 Получение API ключей

1. Перейдите на https://my.telegram.org
2. Войдите с вашим номером телефона
3. Перейдите в "API development tools"
4. Создайте новое приложение
5. Скопируйте `api_id` и `api_hash` в `config.py`

## ⚙️ Конфигурация

Откройте `config.py` и настройте:

```python
# Telegram API (получить на my.telegram.org)
API_ID = 'YOUR_API_ID'
API_HASH = 'YOUR_API_HASH'

# Ваш номер телефона
PHONE = '+79991112233'

# Пароль шифрования локальной БД
ENCRYPTION_KEY = 'your_secure_password_here'

# Настройки Ghost Mode
GHOST_MODE = True
HIDE_ONLINE = True
HIDE_TYPING = True
HIDE_READ = True

# Anti-Delete
SAVE_DELETED_MESSAGES = True
```

## 📖 Использование

### Базовая отправка сообщения

```python
from jirgram import JirgramClient

client = JirgramClient()
client.login()

# Отправка сообщения
client.send_message(chat_id=123456789, text="Привет!")

client.stop()
```

### Ghost Mode

Ghost Mode автоматически активируется при `GHOST_MODE = True` в config.py:

- Скрывает ваш онлайн статус
- Не отправляет статус "печатает..."
- Не отправляет уведомления о прочтении

### История сообщений

Все удаленные и отредактированные сообщения автоматически сохраняются в локальную БД.

## 🛠️ Разработка

### Структура проекта

```
jirgram/
├── main.py              # Основной файл клиента
├── config.example.py    # Пример конфига
├── config.py            # Ваш конфиг (не коммитится)
├── requirements.txt     # Зависимости
├── jirgram/
│   ├── __init__.py
│   ├── client.py        # Логика клиента
│   ├── ghost.py         # Ghost mode функции
│   └── storage.py       # Сохранение сообщений
└── tdlib_files/         # БД TDLib (не коммитится)
```

### Добавление фич

Смотрите [TDLib документацию](https://core.telegram.org/tdlib/docs/) для доступных методов.

## ⚠️ Важно

- **Не коммитьте `config.py`** с вашими credentials
- **Не делитесь API ключами** публично
- Используйте на свой риск
- Уважайте Terms of Service Telegram

## 📄 Лицензия

MIT License - делайте что хотите

## 🤝 Вклад

Pull requests приветствуются. Для больших изменений сначала откройте issue.

## 📞 Контакты

Вопросы? Открывайте issue на GitHub.

---

**Основано на [TDLib](https://github.com/tdlib/td) - официальная библиотека Telegram**
